# Makefile for fdsirqtests
# Note: FDS format test ROMs (no source files)
# This test directory contains pre-built ROMs only.
# No source files available for building.

.PHONY: all clean

all:
	@echo "No source files to build. Pre-built ROMs are in this directory."

clean:
	@echo "No build artifacts to clean."
