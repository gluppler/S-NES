MEMORY {
    ZP:     start = $00,    size = $100, type = rw, file = "";
    OAM:    start = $0200,  size = $100, type = rw, file = "";
    RAM:    start = $0300,  size = $500, type = rw, file = "";
    HDR:    start = $0000,  size = $10, type = ro, file = %O, fill = yes, fillval = $00;
    PRG:    start = $C000,  size = $8000, type = ro, file = %O, fill = yes, fillval = $00;
    CHR: start = $0000, size = $2000, file = %O, fill = yes, fillval = $00;
}

SEGMENTS {
    ZEROPAGE: load = ZP,  type = zp;
    BSS:      load = RAM, type = bss;
    HEADER:   load = HDR, type = ro;
    CODE:     load = PRG, type = ro, align = 256;
    RAMCODE:  load = PRG, type = ro, align = 256, run = RAM, define = yes;
    VECTORS:  load = PRG, type = ro, start = $FFFA;
    TILES:    load = CHR, type = ro;
}
